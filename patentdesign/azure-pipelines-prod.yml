trigger:
- production

pool:
  name: 'IISPool'

pr: none

variables:
- group: iponigeria-production-variables  # Azure DevOps variable group with your secrets
- name: buildConfiguration
  value: Release

stages:
# ---------------------- BUILD STAGE ----------------------
- stage: Build
  displayName: 'Build .NET 8 backend'
  jobs:
  - job: BuildJob
    displayName: 'Build and Publish for Production'
    steps:
    - task: UseDotNet@2
      inputs:
        packageType: 'sdk'
        version: '8.x'
      displayName: 'Install .NET 8 SDK'

    - script: dotnet restore ./patentdesign/patentdesign.csproj
      displayName: 'Restore dependencies'

    - script: dotnet publish ./patentdesign/patentdesign.csproj -c $(buildConfiguration) -o $(Build.ArtifactStagingDirectory)/backend
      displayName: 'Build & publish backend'

    - task: PublishBuildArtifacts@1
      inputs:
        PathtoPublish: '$(Build.ArtifactStagingDirectory)/backend'
        ArtifactName: 'backend'
        publishLocation: 'Container'
      displayName: 'Publish build artifacts'

# ---------------------- DEPLOY STAGE ----------------------
- stage: Deploy
  displayName: 'Deploy to IIS'
  dependsOn: Build
  jobs:
  - job: DeployJob
    displayName: 'Deploy Application (Production)'
    steps:
    - download: current
      artifact: backend

    # ---------------------- 1️⃣ Inject env vars into IIS App Pool ----------------------
    - task: PowerShell@2
      displayName: 'Inject environment variables into IIS App Pool'
      inputs:
        targetType: 'inline'
        script: |
          Import-Module WebAdministration

          $appPool = "integration.iponigeria.com"

          $envVars = @{
              MONGODB_CONNECTION_STRING = "$(MONGODB_CONNECTION_STRING)"
              JWT_KEY                   = "$(JWT_KEY)"
          }

          Write-Host "Clearing existing environment variables for App Pool: $appPool"
          Clear-WebConfiguration -Filter "/system.applicationHost/applicationPools/add[@name='$appPool']/environmentVariables" -PSPath "IIS:\" -ErrorAction SilentlyContinue

          Write-Host "Injecting environment variables into App Pool: $appPool"
          foreach ($name in $envVars.Keys) {
              Add-WebConfigurationProperty -PSPath "IIS:\" `
                  -Filter "/system.applicationHost/applicationPools/add[@name='$appPool']/environmentVariables" `
                  -Name "." `
                  -Value @{name=$name; value=$envVars[$name]}
              Write-Host " - $name set"
          }

    # ---------------------- 2️⃣ Stop App Pool, deploy files ----------------------
    - task: PowerShell@2
      displayName: 'Stop App Pool, clear old files & deploy new'
      inputs:
        targetType: 'inline'
        script: |
          Import-Module WebAdministration

          $appPool = "integration.iponigeria.com(domain)(pool)"   # ← must match step 1
          $source = "$(Pipeline.Workspace)\backend\*"
          $destination = "C:\inetpub\vhosts\iponigeria.com\integration.iponigeria.com"

          # --- Stop the App Pool ---
          Write-Host "Stopping App Pool: $appPool"
          if ((Get-WebAppPoolState -Name $appPool).Value -ne "Stopped") {
              Stop-WebAppPool -Name $appPool
          }

          # --- Wait for worker processes to fully exit ---
          $maxWait = 30  # seconds
          $elapsed = 0
          while ($elapsed -lt $maxWait) {
              $workers = Get-ChildItem "IIS:\AppPools\$appPool\WorkerProcesses" -ErrorAction SilentlyContinue
              if (-not $workers -or $workers.Count -eq 0) {
                  Write-Host "Worker processes exited after $elapsed seconds."
                  break
              }
              Write-Host "Waiting for worker processes to exit... ($elapsed s)"
              Start-Sleep -Seconds 2
              $elapsed += 2
          }

          # --- Kill any remaining w3wp processes for this pool ---
          if ($elapsed -ge $maxWait) {
              Write-Host "Force-killing remaining worker processes."
              $workers = Get-ChildItem "IIS:\AppPools\$appPool\WorkerProcesses" -ErrorAction SilentlyContinue
              foreach ($w in $workers) {
                  Stop-Process -Id $w.processId -Force -ErrorAction SilentlyContinue
              }
              Start-Sleep -Seconds 2
          }

          # --- Remove old files ---
          Write-Host "Removing old files in $destination"
          Remove-Item -Path "$destination\*" -Recurse -Force -ErrorAction SilentlyContinue

          # --- Copy with retry ---
          $retries = 3
          for ($i = 1; $i -le $retries; $i++) {
              try {
                  Write-Host "Copying new build (attempt $i of $retries)..."
                  Copy-Item -Path $source -Destination $destination -Recurse -Force -ErrorAction Stop
                  Write-Host "Copy succeeded."
                  break
              } catch {
                  Write-Host "Copy failed: $_"
                  if ($i -eq $retries) { throw }
                  Start-Sleep -Seconds 5
              }
          }

          # --- Start the App Pool ---
          Write-Host "Starting App Pool: $appPool"
          if ((Get-WebAppPoolState -Name $appPool).Value -ne "Started") {
              Start-WebAppPool -Name $appPool
          }