{
  "PatentDesignDatabase": {
    "ConnectionStringUp": "$(MONGODB_CONNECTION_STRING)"
  },
  "EmailSettings": {
    "SmtpServer": "$(SMTP_SERVER)",
    "Username": "$(SMTP_USERNAME)",
    "Password": "$(SMTP_PASSWORD)"
  },
  "Jwt": {
    "Key": "$(JWT_KEY)"
  }
}